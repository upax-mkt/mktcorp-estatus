import { Cargando } from '@/componentes/Cargando'

/** La tabla de acuerdos cruza las diez salas antes de pintar una sola fila. */
export default function Cargar() {
  return <Cargando filas={6} />
}
